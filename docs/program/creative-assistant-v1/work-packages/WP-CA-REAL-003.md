# WP-CA-REAL-003: Copied real-project immutable Original rebinding

## Outcome

Make the repository-external real Product fixture establish a fresh immutable
Original inside its copied review project before it assembles new Material
Evidence. Repair the Worker audio composition defect exposed only when the
corrected complete-duration Product fixture places multiple contiguous clips
from the same audible Original. Do not weaken the Host rule that immutable
Originals are valid only at their owning project's internal path or relax QC.

## Scope and invariants

The repair is limited to the real Product test harness. After the project copy,
it must obtain a new exact material-permission decision for the copied
project's current `original` location, which causes the existing Host flow to
create and authorize that project's own immutable Original. SHA-256, source
permission, Contract policy binding, original media bytes and all Host
fail-closed checks remain unchanged.

The Worker repair is limited to the same-track multi-clip embedded-audio
composition path: do not pad individual delayed clips before `amix`; mix their
exact placed spans first, then use the existing final output padding. Add a
Worker regression that proves two contiguous audible clips stay audible across
their shared boundary. Because this changes encoded output bytes, advance the
single current internal `worker-media` adapter identity in its Contract source,
generated bindings, cache key, Host and storage validation together; existing
v4 bundles are not reusable as v5 results.

## Validation

Reproduce the former stale-immutable failure and the full-duration `SILENCE`
failure with the authorized external project, then run the Worker correctness
regression, real Product Electron route, synthetic Product suite, typecheck and
documentation gates. A passing repair returns to `WP-CA-REAL-001` for
direct-human review and the remaining Stage Exit work; it does not claim those
gates itself.

The authorized 65-second Product fixture selects the exact audible source
window from 2 through 62 seconds: its existing silent lead-in must not be
misclassified as an audio compiler defect or exempted by relaxing QC. Original
bytes and permissions are unchanged; selected Evidence and Story remain exact
60-second unit-speed ranges.

The disposable copied fixture rebuilds its review Timeline through ordinary
Host Commands using the existing canonical desktop track definitions before
creating new Story/execution artifacts. The Pipeline-only source topology is
not a desktop project layout. Production open validation remains unchanged;
no project migration or compatibility reader is introduced.

The authorized Electron harness repair selects an explicit current feedback
target, submits a valid exact one-second inward trim, and permits only its
exact creation and subsequent rejection dialogs once each. Mismatched or
repeated confirmations fail closed; production dialogs remain unmodified.
The journey waits for enabled controls and compares historical stale IDs,
current execution, feedback decision and workspace identity across reopen.
