# WP-CA-MERGE-020 WP019 replay-command governance correction

## Outcome

Correct the completed WP019 test-command vocabulary so every declared gate is
an executable repository script and the final Evidence remains reproducible.

## Scope

- Replace the nonexistent `desktop:workbench-host:test` declaration with the
  verified `workbench:host:test` script.
- Refresh shared fingerprints, matrices, State, Evidence and generated current
  documents without changing capability status.

## Non-goals

- No application, package, test, contract, storage or Worker changes.
- No capability or acceptance promotion.
- No PR merge authorization.

## Validation

Run the corrected focused command, desktop boundary, docs/fingerprint checks,
Stage 2, full repository and synthetic final gates, followed by independent
review and exact-head remote checks.
