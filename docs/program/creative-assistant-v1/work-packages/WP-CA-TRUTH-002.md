# WP-CA-TRUTH-002 Exact current storage and navigation closure

## Outcome

Close the final independent-review findings after `WP-CA-TRUTH-001`: every
persisted Render is a complete current ExecutionPlan bundle, every same-number
project database has the exact current baseline schema, and repository
navigation states the fresh Stage 2 acceptance boundary.

## Required behavior

- Project open compares the complete SQLite schema to the current baseline and
  rejects any same-number but different layout before authoritative database or
  project-state writes; its temporary ownership lock is released on rejection.
- Project Storage exposes one Render Bundle writer only. It validates two
  `worker-media@v4` plans, two `ave-worker-host-r14` outputs/results, exact
  Preview/Master linkage and output hashes before its transaction.
- Tests use complete current bundles and prove old, partial and rebound
  identities cause zero persistence.
- Root and contract navigation link the current policy and Creative Stage 2
  programme, and state that earlier real/human Evidence cannot satisfy the
  corrected complete-duration gate.

## Non-goals

No real-media acceptance, capability expansion, old-project conversion or PR
merge.

## Validation

Run storage, Render Bundle, Stage 2 Product, contract, type, architecture and
documentation gates plus independent read-only review.
