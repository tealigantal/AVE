# WP-S3-INTEGRATION-001: First real personalized creation checkpoint

## Outcome and authority

This programme Work Order promotes only the first integrated slice of S3-01 through
S3-05, with S3-06 evidence collection, under the user's 2026-09-18 checkpoint
instruction. It does not complete those candidate packages or Stage3 Exit.
The manifest is the sole allowed-path and dependency authority. Navigation is
the Stage3 Work Order index and generated programme index. A new programme
preserves the completed Stage2 package history and its still-open acceptance debts.

## Integration order and ownership

The root agent is the only source writer. Read-only analysis/review inherits
the selected GPT-6 High settings. One package remains active globally.

1. S3-01 fixes request authority, immutable revisions, cancellation generations,
   persistent drafts and independent adopted/viewed pointers; validate races.
2. Integrate typed feedback and the single local profile owner, real model
   generation and Host compilation, then approved-workspace consumers.
3. Run the first C1/C2/C3/C6 path as soon as its executable seams exist, including
   applicable C4/C8/C9. Preserve each failure before repairing code or inputs.
4. Verify real renders/QC/reopen, focused tests and repository gates; independent
   review fixes; only then commit/push the task branch and create a draft PR.

Dependencies inside this integration package are ordered implementation
milestones, not fabricated completed S3 packages. WP-CA-REAL-003 supplies the
existing verified media/Host substrate; it does not prove Stage2 formal Exit.

## Registered scope clarifications

Shared Host/Storage/API paths are enumerated in the manifest. Actual preload
consumers are `preload.ts` and `preload-runtime.cjs`, not the nonexistent
candidate `preload/` directory. The code generator's explicit validator registry
may be updated for the current contract family; checks themselves are not modified.
Generated contracts and current documentation are generated only.
The current project v2 object/reference/event tables are reused where possible;
database format changes require an explicit scope update before editing.
Existing programme matrices/state may append fresh regression Evidence when
shared-code changes invalidate their scopes; never rewrite historical Evidence,
old work-package definitions, or claim fresh human acceptance.

2026-09-24: the render cancellation path requires a real Worker process-tree
termination barrier. The existing worker-client scope includes its local process
owner and declaration update. Before changing stop to an awaited operation, the
manifest additionally registers its four existing direct consumers:
`apps/dev-cli/src/main.ts`, `tests/integration/platform-foundation.test.ts`,
`tests/integration/worker-crash-recovery.test.ts`, and
`tests/integration/worker-client.test.ts`. Changes there are limited to awaiting
the same stop contract and retaining failure causes. This is part of the first
creation loop's file-producer lifetime, not a new package or CI/tooling rewrite.

2026-09-24: the learning integration additionally registers
`packages/platform/project-host/src/stage3-learning.ts` for the Host-owned typed
projection of selected historical records and bounded model decision validation.
It introduces no independent database owner. Profile coordination, contracts,
project object/reference storage and Stage3 test paths are already registered.
Exact historical source selection, unconditional learning-send authorization,
recoverable cross-database registration and explicit no-inference outcomes belong
to the same first-loop scope; no candidate package is separately completed.

2026-09-24: Main bootstrap, app/window lifecycle and Electron declarations, plus
the existing Electron harness and review consumers, are registered for the first
loop's single profile owner and safe task lifetime. Main captures project/session
identity through native waits, drains actual handlers and closes Host before the
profile. Retryable storage cleanup is in the existing Host/storage allowed paths.
The desktop-boundary checker remains required; any later assertion migration must
preserve its denial coverage when the single current product entry is replaced.

## Required result and stopping boundary

Retain actual model calls, raw user statements, multi-shot sound/caption changes,
correction and preserved content, held-out project use, one-off exception and
forgetting effects, saved/reopened versions, actual Preview/Master and QC.
Private material, private model contexts and sensitive logs stay outside Git.
Missing exact design blocks visual claims; missing material/data/cost consent
blocks real calls, not independent implementation. No invented sunset/dialogue,
equal splitting or fixed reordering represents material understanding.

Completion means the bounded checkpoint is reviewable, with final head and real
artifact/recording locations. Stop for external review; no merge, release or
next checkpoint. External blockage remains incomplete with Evidence and Debt.

2026-09-24: register `packages/platform/project-host/src/stage3-workspace.ts`
for the safe Host-owned persistent workspace projection. It composes existing
validated project records and one coordinated profile read, without a new
state owner, database or arbitrary object-reading API. Existing storage/profile,
public exports and Stage3 tests cover its required readers and verification.

2026-09-24: register `tests/integration/stage2-material-run.ts` before removing
its dependency on the production Stage2 topology helper. Historical Stage2
fixtures remain test-owned. Existing timeline-core scope includes the typed
persistent Timeline structure boundary; Host validates exact storage/version
identity and creation references before recovery writes. Desktop new/open, IPC,
Renderer and their existing tests must migrate coherently, without a second
current product workflow or automatic conversion of old project semantics.

2026-09-24: additionally register `tests/fixtures/stage2-confirmation.ts` to
retain the historical Stage2 domain confirmation regression fixture while
removing that adapter from production Main. Current Desktop denial checks
migrate to exact Stage3 authorization and session-bound native confirmation.
This does not retain a second runtime entry or waive any current gate.

2026-09-24: register `tests/architecture/ipc-boundary.mjs` for the exact current
handler-name assertion migration, preserving sender, preload, projection and
removed-route denials. Register `tests/integration/stage2-material-case.test.ts`
for its direct Electron review consumer when replacing the current workflow;
historical material-case domain checks remain required and must not masquerade
as a current Stage3 product journey. No checker execution or bypass rule changes.

2026-09-24: register `tests/integration/stage2-real-entry.test.ts` before
separating the retired Desktop real-entry failure from the two retained Stage2
domain material gates. ADR-0028 forbids manufacturing request authorization for
historical projects. The retired product command must fail before reading media
or creating output; new Creation Electron tests inherit the actual playback,
exact confirmation, rejected-write, retained-input and reopen assertions.


2026-09-24 resumed scope: the user selected the latest Desktop design archive
ave-v12-5-1-apple-cn-refined (SHA-256 858d8d3ed5b97f1c3d50bafe7a71b638b673160a50a718540a62534b0cb54710).
Historical approved Stage2 media and exact output hashes were located again;
local learning rules remain authoritative. User explicitly rejects product data,
call and cost ceilings. Register model-configuration.ts, ADR-0029 and its index
before implementing a single current accounting/capability contract replacement.
Keep exact send consent, durable call auditing, finite technical retries,
cancellation and Host authority. No real provider call or new acceptance yet.
The same WP is active; previous blocked-input conclusion is superseded by this
resumption, not retrospectively rewritten. Main remains the only profile owner.

2026-09-24 user-selected split-service slice: Qwen vision and text planning, Whisper-compatible multipart transcription, and an independent native-audio chat model for environmental sound. Root is sole writer. Register split-provider.ts, whisper.ts and ADR-0030 before edits. Retain existing schema/Host/Main/Renderer/test allowed paths. Deliver editable local JSON settings and example, real HTTP protocol implementations, per-subcall durable target ledger, exact sample/source time fusion, errors/cancellation/zero-commit tests and current required gates. No download, paid request, or real creative acceptance is implied. No candidate package promotion.


Split-service implementation checkpoint (2026-09-24): added the ordered fusion
proof and shared exact-time reconstruction in contract-runtime, registered before
source writes, plus model-service setup instructions. The one active WP remains
unchanged. Physical calls are individually authorized/settled; split outer audit
cannot overwrite child usage during cancellation. Planner retains its single-call
usage. Root-only fixes followed read-only provider review.
Targeted model/Host observation/Desktop regressions, TypeScript and architecture
passed. The local test includes actual PNG/WAV encoding, multipart/SSE, generation
and reopen; provider responses are controlled fixtures. Earlier type failures,
parser failures, incorrect test wire-count assumption, the original aggregate
result accounting failure and missing transcript test consent remain in OS TEMP
logs. Current source will undergo full check and final synthetic acceptance next.
No task commit, real provider call, deployment or package completion occurred.
