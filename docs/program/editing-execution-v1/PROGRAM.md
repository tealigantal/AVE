# Editing Execution v1 Program

The complete v1 scope is declared in `../../product/EDITING_CAPABILITY_SCOPE_V1.md`; this programme only sequences it. All capabilities enter the manifest now. A dependency never removes scope. P0 reliable-media evidence remains a baseline, while every v1 capability begins `specified` until its own evidence supports a stronger status.

The resolver/adapter design is an architectural target. Implementing packages must retain Project Host, Contract, RationalTime, Command/Commit and Preview/Master invariants.
