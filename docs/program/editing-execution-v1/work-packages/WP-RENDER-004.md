# WP-RENDER-004: PR #5 merge-blocking semantic correctness fixes

## Goal

Resolve the final-head correctness findings for transition execution, independent transform axes, pitch policy and authoritative Timeline duration.

## Allowed paths

- `packages/core/render-graph/**`
- `apps/worker-host/**`
- `tests/**`
- `docs/**`
- `scripts/docs/**`

## Required evidence

An `EVD-*` record with focused Host/Worker/media assertions, the complete check result and the final code fingerprint.
