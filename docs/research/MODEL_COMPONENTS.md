# Model Components

SAM 2 and Depth Anything V2 are unverified candidate providers for segmentation/depth assets. Any model must run behind the AI Asset Backend, record model/version/input provenance/confidence, allow manual correction, and never write Timeline or SQLite. Availability, quality, privacy, cost and licensing remain unverified until official documentation is recorded.
