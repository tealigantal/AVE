# Withdrawn Advanced-Family Video Acceptance

This ExecPlan is the terminal delivery record for blocked `WP-ADV-002`.

## Purpose / Big Picture

The original plan attempted to represent `ACC-001` through `ACC-011` with
eleven separately encoded videos. On 2026-08-13 repository evidence showed
that the suite promoted bounded approximations and FFmpeg-plan markers into
full-family claims. The driver was physically removed. On 2026-08-23 the
programme pointer was corrected so this rejected premise cannot remain the
active task.

## Progress

- [x] Confirm that the accepted combined showcase did not close `ACC-001`
  through `ACC-011`.
- [x] Remove the misleading eleven-family real-acceptance driver and script.
- [x] Retain valid domain, encoded-output, reopen and failure-closure tests.
- [x] Publish the bounded Tool Usability Registry and PRECHECK Evidence.
- [x] Confirm from current code that automation execution is limited to x/y,
  anchor/original-size explicitly block, and static reframe is manual.
- [x] Withdraw the eleven-video outcome without shrinking v1 scope.
- [x] Move the active programme pointer to the narrow `WP-KF-002` successor.

## Surprises & Discoveries

- The deleted family suite principally asserted filter-plan strings rather
  than the complete visible semantics named by each professional capability.
- The current renderer emits automation nodes only for `transform.x` and
  `transform.y`; other registered properties remain explicit blockers.
- Static `crop_fill`, `contain`, and `blurred_background` with a manual focal
  point are real bounded tools. Subject detection and dynamic reframe are not.

## Decision Log

- 2026-08-13: Never rename Pixelize as Luma Wipe, rectangular alpha as subject
  segmentation, or segmented constant speed as a continuous speed ramp.
- 2026-08-13: Delete the misleading driver rather than archive it as a reusable
  acceptance path.
- 2026-08-23: Mark `WP-ADV-002` blocked instead of completed. The original
  acceptance requirements did not pass.
- 2026-08-23: Start with a new bounded transform-automation acceptance because
  the existing Timeline, RenderGraph and Worker provide a concrete x/y seam.
  Keep `ACC-001`, subject-aware reframe and the remaining families as Debt.

## Outcomes & Retrospective

The useful bounded tools and their tests remain. The invalid full-family video
claim is no longer active, and no `CAP-*` or `ACC-001` through `ACC-011` status
was promoted. Future completion must come from capability-specific work.

## Context and Orientation

The acceptance authority is
`docs/program/editing-execution-v1/ACCEPTANCE_MATRIX.yaml`. Current blockers
are in `STATE.yaml` and generated `docs/current/DEBT.md`. The bounded registry
is `docs/specifications/editing-execution-v1/TOOL_USABILITY_REGISTRY.md`.

## Plan of Work

This plan has no remaining implementation milestone. Resume from the active
successor plan named by generated `docs/current/WORK.md`.

## Concrete Steps

Do not run or recreate an advanced-family video suite. Use the successor work
package's exact focused commands and acceptance boundary.

## Validation and Acceptance

The terminal claim is only that the old package is blocked and withdrawn. The
bounded tool registry remains machine-tested; the broad families remain
blocked until their own acceptance assertions pass.

## Idempotence and Recovery

Programme regeneration must preserve `WP-ADV-002` as blocked. Re-running
`docs:sync` must not reactivate it or recreate deleted media drivers.

## Artifacts and Notes

The authoritative retained record is
`EVD-20260823-WP-ADV-002-BLOCKED`. Repository-external historical media is not
required for, and cannot reverse, this governance conclusion.

## Interfaces and Dependencies

No runtime interface changed. `WP-KF-002` depends on the accepted foundation
and advanced showcase paths, not on this blocked package.
