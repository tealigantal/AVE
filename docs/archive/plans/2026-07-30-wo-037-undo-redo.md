<!-- HISTORICAL ARCHIVE: retained evidence; not current authority. -->

# WO-037 Undo/Redo

## Outcome

Undo 使用前一版本快照和最近命令生成逆命令并提交新版本；Redo 复用原始命令提交路径。

## Validation

- `npm run undo-redo:test`
- `npm run check`
- `npm run p0:acceptance`

## Evidence

Add → Undo → Redo 依次产生 v1、v2、v3；所有变更均走 `applyCommand`/`commitTimeline`，无可用历史时失败。

## Remaining Risk

Redo 栈当前是 Project Host 会话内状态，尚未在关闭重开后恢复；Renderer 尚未提供 Undo/Redo 控件。
