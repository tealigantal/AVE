---
evidence_id: EVD-20260803-WP-RENDER-004
date: 2026-08-03
work_package_id: WP-RENDER-004
repository_commit: 16cff03fa4cea440589a6471fbecd6415caacc74
code_fingerprint: ea926d31be31d247f52c1a3f5facdf3be006070111994476d13a001dbb90df1f
capability_ids: [CAP-TL-001, CAP-KF-001, CAP-XFORM-001, CAP-COMP-001, CAP-TIME-001, CAP-TRANS-001, CAP-COLOR-001, CAP-MASK-001, CAP-TEXT-001, CAP-AUDIO-001, CAP-RENDER-001]
acceptance_ids: [ACC-001, ACC-003, ACC-004, ACC-012, ACC-014, ACC-015]
commands: ["pnpm run render-graph:test", "pnpm run timeline-render:test", "pnpm run worker:render-graph:test", "pnpm run worker:render-correctness:test", "pnpm run worker:python:lint", "pnpm run worker:python:typecheck", "pnpm run typecheck", "pnpm run docs:sync", "pnpm run docs:check", "pnpm run check"]
result: passed_with_explicit_transition_and_change_pitch_blockers
environment: "Windows local checkout; Node 22.16.0; pnpm 11.9.0; Python 3.11.8; FFmpeg/ffprobe 7.1.1"
artifacts: ["packages/core/render-graph/src/v2.ts", "apps/worker-host/src/worker_host/render/graph_compiler.py", "tests/property/render-graph.test.ts", "apps/worker-host/tests/render_graph_protocol_smoke.py", "apps/worker-host/tests/render_graph_media_correctness.py", "tests/integration/timeline-render.test.ts"]
remaining_risks: ["Transition execution remains an explicit blocker until source-handle semantics are implemented.", "Change-pitch time-map execution remains an explicit blocker.", "Authorized real-media final acceptance remains blocked by DEBT-RENDER-002-E."]
---

# PR #5 Merge-blocking Correctness Evidence

The final-head findings were corrected without silently expanding the supported adapter surface.

- Project Host blocks every transition until real source-handle execution can preserve both requested clip ranges; Worker independently rejects transition nodes and the old `xfade` plus cloned-tail path was removed.
- Project Host blocks `pitch_policy: change`; Worker independently rejects it instead of applying preserve-pitch `atempo` semantics.
- Worker executes `scale_x` and `scale_y` independently, defaults the omitted axis to 1 and rejects invalid single-axis values.
- Worker validates and honors the Host-declared Timeline total duration, including trailing gap or caption-only ranges.
- Project Host persistence proves Preview and Master blocker plans for an unsupported transition.
- Encoded-media acceptance proves the trailing duration and black timeline range are present in the final output.

Focused checks and the complete `pnpm run check` repository matrix passed. No user media was copied or committed.
