---
evidence_id: EVD-20260802-WP-RENDER-002
date: 2026-08-02
work_package_id: WP-RENDER-002
repository_commit: ba8a6dd9f08d50846735ad63aea88b16ce896b14
code_fingerprint: 64437ea9be26fd6efd6d5855f44e3e657bae55dec883d875a7cd08458a846185
capability_ids: [CAP-TL-001, CAP-KF-001, CAP-XFORM-001, CAP-COMP-001, CAP-TIME-001, CAP-TRANS-001, CAP-COLOR-001, CAP-MASK-001, CAP-TEXT-001, CAP-AUDIO-001, CAP-RENDER-001]
acceptance_ids: [ACC-001, ACC-002, ACC-003, ACC-004, ACC-005, ACC-006, ACC-007, ACC-008, ACC-009, ACC-010, ACC-011, ACC-012, ACC-013, ACC-014, ACC-015]
commands: ["pnpm install --frozen-lockfile", "pnpm run docs:sync", "pnpm run docs:fingerprint:test", "pnpm run contracts:generate", "pnpm run contracts:check", "pnpm run contracts:compatibility", "pnpm run contracts:clean", "pnpm run typecheck", "pnpm run architecture", "pnpm run render-graph:test", "pnpm run timeline-core:test", "pnpm run commit-plan:test", "pnpm run proxy-time-map:test", "pnpm run timeline-render:test", "pnpm run render-bundle:test", "pnpm run render-persistence:test", "pnpm run timeline:audio-caption:test", "pnpm run timeline:host:test", "pnpm run worker:python:lint", "pnpm run worker:python:typecheck", "pnpm run worker:render-graph:test", "pnpm run worker:render-correctness:test", "pnpm run worker:qc:test", "pnpm run storage:check", "pnpm run dev-cli:test", "pnpm run acceptance:final:synthetic", "pnpm run docs:check", "pnpm run docs:architecture:test", "pnpm run check"]
result: passed_with_explicit_blocked_scope
environment: "Windows local checkout; Node 22.16.0; pnpm 11.9.0; Python 3.11.8; FFmpeg/ffprobe 7.1.1; ruff 0.6.9; mypy 1.17.1"
artifacts: ["contracts/schemas/render-execution-plan.v1.schema.json", "packages/core/render-graph/src/v2.ts", "packages/platform/project-host/src/project-host.ts", "packages/platform/project-storage/src/project-storage.mjs", "apps/worker-host/src/ave_worker/execution_plan.py", "apps/worker-host/src/ave_worker/graph_compiler.py", "database/migrations/0019_render_bundles.sql", "tests/integration/render-bundle.test.mjs", "apps/worker-host/tests/render_graph_media_correctness.py"]
remaining_risks: ["Real-media final acceptance is blocked because the repository has no authorized fixture and AVE_REAL_MEDIA_PATHS/AVE_REAL_SUBTITLE_PATH are unset.", "Nested/compound/adjustment, Bezier/dynamic transform, ellipse/tracked masks, complete transition/color/graphics, variable remap, ducking/loudness and MLT execution remain explicit blockers recorded in STATE debt; their CAP/ACC entries are not tested claims."]
---

## Corrective Scope

This Evidence audits and hardens the PR #4 RenderGraph v2 execution path from committed Timeline through target-neutral semantic graph, Project Host resolution, mandatory Worker ExecutionPlan validation, FFmpeg execution, output verification, atomic Render Bundle registration and close/reopen recovery. It also reconciles earlier machine-readable `tested` claims that exceeded implemented v1 semantics.

## Contract and Determinism Results

- Contract generation produced 82 TypeScript/Python bindings from 41 schemas. All 41 valid examples and 41 invalid examples behaved as declared; compatibility found 41 unique v1 IDs; TypeScript JSON parse to Python JSON parse to JSON Schema round trip passed for all valid examples; generated-tree cleanliness passed.
- Canonical serialization recursively sorts object keys, preserves arbitrary-precision integers through an explicit tag, normalizes negative zero and rejects non-finite, undefined and cyclic values.
- Preview and Master use one identical target-neutral semantic manifest/hash. Target, profile, range, adapter version and sorted source/input identities are included in target-specific cache keys and plan IDs.
- Worker rejects a missing ExecutionPlan, semantic-hash tampering, target mismatch, cache mismatch and incomplete resolver-decision coverage before FFmpeg compilation.

## Timeline and Resolver Results

- Timeline property checks passed 250 generated commands over 18 command kinds. TimeMap tests passed 200 generated valid maps plus exact boundary, uniqueness, rational-ratio and failure-atomicity assertions.
- CommitPlan tests verified real pre/post affected ranges and atomic failure. Nested/parent cycles, global IDs, compound membership, transitions, automation paths, masks, color and track state are structurally validated.
- RenderGraph property tests verified disabled/muted/solo filtering, audio-enabled behavior, deterministic 40-track z-order, unsupported automation/tracked-mask/anchor blockers and complete per-node resolver-decision coverage.
- Unsupported semantics are persisted in a blocked bundle and never submitted to Worker. No shallow fallback or silent node omission is accepted.

## Encoded Synthetic Media Results

`worker:render-correctness:test` created temporary synthetic red/blue video with 440 Hz audio and a delayed green PiP with 880 Hz audio, then executed the real FFmpeg compiler and probed the encoded outputs.

- Pixel samples were red before the delayed PiP, green during it and blue after it, proving Timeline placement, transparent layer composition and base continuity.
- Frequency analysis found the 440 Hz base throughout and 880 Hz only during the delayed overlay, proving Timeline-aware per-track/cross-track audio mix rather than global concatenation.
- ffprobe confirmed expected dimensions, video/audio stream presence and exact bounded output durations.
- 2x output was 2 seconds; 0.25x output was 4 seconds and exercised multi-stage audio tempo; hold output was 1 second with at least 29 frames at the selected blue source point; reverse output was 4 seconds with blue-first/red-last ordering and synchronized audio.
- Timeline integration, audio/caption rendering, Master QC and synthetic final acceptance also passed. `acceptance:final:synthetic` explicitly reported that real media was not claimed.

## Atomic Render Bundle and Recovery Results

- A completed bundle required exactly one render run, Preview and Master results, two plans and two output manifests. A blocked bundle required two plans plus diagnostics and contained no render run or media output.
- Host verified plan ID, semantic hash, cache key and output SHA-256 before registration; Storage copied verified media into immutable content-addressed objects.
- Injected failures at render, result and manifest insertion rolled back all SQLite rows and removed newly staged orphan objects.
- Same-content idempotent retry returned the original bundle; conflicting content under the same key failed closed. Close/reopen recovered the same bundle and immutable object references.
- Migration 0019, WAL/object atomic write, storage lifecycle, render persistence and schema-19 development CLI flow passed.

## Repository Fingerprint and Governance

The fail-closed fingerprint uses Git's tracked plus untracked/non-ignored source list, normalizes CRLF/LF, frames path/content lengths, includes Unicode/space paths, invalidates on untracked source, excludes ignored files and fails on missing tracked files or Git discovery failure. Machine-readable Debt rendering also passed. After the final review-fix code commit, the observed code fingerprint was `64437ea9be26fd6efd6d5855f44e3e657bae55dec883d875a7cd08458a846185`.

After work-package completion and document generation, `pnpm run docs:check`, the documentation structure/governance negative suite, and the entire repository `pnpm run check` command completed with exit code 0. The full command reran the workflow contract, all architecture/boundary suites, contract roundtrip/cleanliness, Worker lint/type/media/QC/correctness, storage/bundle/recovery, object-store, Host APIs, Timeline, render persistence and timeline render tests.

## Real Media

Blocked, not failed and not substituted. Repository fixture search found no authorized media files. `AVE_REAL_MEDIA_PATHS` and `AVE_REAL_SUBTITLE_PATH` were both unset. No user media was uploaded, copied, committed or represented as synthetic evidence. `tests/integration/real-media-final-acceptance.test.ts` remains the Project Host acceptance lane once authorized local AV and subtitle paths are supplied.

## Capability and Acceptance Reconciliation

`CAP-RENDER-001` and `ACC-012` through `ACC-015` are tested by this evidence. `CAP-TL-001` through `CAP-AUDIO-001` and `ACC-001` through `ACC-011` are blocked wherever their complete v1 scenario depends on unimplemented semantics. Partial encoded results are recorded in their notes but do not promote the broad capability. Five active/blocked Debt entries define explicit exit conditions.

## User Media Copied or Committed

No.

## Replacement PR and Remote CI

Replacement PR: `https://github.com/tealigantal/AVE/pull/5`. It fully contains PR #4 and the hardening commits. PR #4 remained open and received the required supersession comment. On the initial PR head, Acceptance `p0`, Architecture `boundaries`, CI `check`, Contracts `contracts`, Golden and Integration `golden`, Security `audit`, Worker `protocol`, and CodeRabbit status all completed successfully. CodeRabbit reported that its new review was skipped because the PR contains 101 changed files, so the original #4 inline findings were instead independently audited against the replacement implementation and regression tests.

The Codex connector review of commit `3558cc1` reported four additional findings. All were validated and fixed in `ad2ce06` and `ba8a6dd`: Timeline coordinates now derive from the root Sequence RationalTime instead of the first source timebase (including exact non-unit tick scaling); Worker binds and hashes the exact Host canonical payload while independently rebuilding and structurally comparing the semantic/cache objects, eliminating JavaScript/Python exponent-spelling drift without weakening tamper detection; unregistered effects and invalid effect parameters now become persisted resolver blockers instead of throwing before bundle creation; ellipse masks resolve to `ELLIPSE_MASK_RENDER_UNSUPPORTED`, with a defensive Worker rejection, instead of being rendered as their rectangular bounds. Regression tests cover 1/1000 and 1001/30000 sequence timebases, `1e-7`/`0.000001` cross-language numeric spellings, semantic numeric tampering, both target plans for an unregistered-effect blocked bundle, Host ellipse resolution and Worker ellipse rejection.

After all four corrections, `docs:sync`, `docs:check`, `acceptance:final:synthetic` and the complete `pnpm run check` suite were rerun and passed. The final code fingerprint is `64437ea9be26fd6efd6d5855f44e3e657bae55dec883d875a7cd08458a846185`.
