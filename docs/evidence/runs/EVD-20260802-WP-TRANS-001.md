---
evidence_id: EVD-20260802-WP-TRANS-001
date: 2026-08-02
work_package_id: WP-TRANS-001
repository_commit: ae07ad8
code_fingerprint: 8f3761670710d662fcc5a252f3f0e958709688cc0062c730a2e7b931d120a414
capability_ids: [CAP-TRANS-001]
acceptance_ids: [ACC-003, ACC-015]
commands: ["pnpm run render-graph:test", "pnpm run worker:render-graph:test", "pnpm run worker:python:lint", "pnpm run typecheck"]
result: passed
environment: Windows local checkout
artifacts: ["packages/core/render-graph/src/effect-registry.ts", "apps/worker-host/src/worker_host/render/graph_compiler.py"]
remaining_risks: ["Unregistered effects and transitions deliberately block; future registry entries require their own backend mapping and evidence."]
---

## Scenario

Resolve registered effects and execute Whip, Zoom, Dissolve and Luma Wipe transitions using bounded FFmpeg mappings; reject unregistered effects rather than silently omit them.

## Actual Observable Results

The render-graph property test accepts registered blur and rejects an unknown effect. The Worker protocol acceptance renders each required transition and asserts its compiled `xfade` mapping (`hblur`, `zoomin`, `fade`, `pixelize`); invalid backend semantics raise a structured `EFFECT_UNSUPPORTED` or `TRANSITION_UNSUPPORTED` error.

## Real Media

No; generated fixtures only.

## User Media Copied or Committed

No.
