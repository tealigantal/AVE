---
evidence_id: EVD-20260825-WP-CA-UX-001-R2-COMPLETE
date: 2026-08-25
work_package_id: WP-CA-UX-001
repository_commit: worktree-stage2-ux-evaluation-r2-complete
code_fingerprint: 20d4108635acb92b51b518a98e9e40203583c772a47ca27a023ffb4f23fa5f87
capability_ids: [CAP-CA-UX-001]
acceptance_ids: [ACC-CA-UX-001]
commands: ["pnpm run stage2-product-workspace:real with explicit repository-external source and fresh v21 review root", "root-agent visual inspection of all current views and recovery capture", "pnpm run check", "pnpm run docs:sync", "pnpm run docs:check", "git diff --check", "independent read-only P0/P1/P2 review"]
result: passed_complete
environment: "Windows local checkout; exact authorized real source; isolated real Electron/Chromium v21 review project; evaluation-only docs scope; no deployment or publication"
artifacts: ["four same-version Contract Evidence Direction and Review views", "two Direction and two Story candidates", "current three-second Preview played in Electron", "execution-bound Preview Master and passed QC", "new scoped local feedback preview", "dual-ID action closed before writes", "visible explicit rejection with exact Permission Decision", "Timeline unchanged by rejection", "undo v7 redo v8", "exact reopen digest f3cb2797ec5184af1ee095fee5a4093d3a53a63949994c2edc5e31a93fbd2912", "stale Render media preview and feedback visibly closed", "complete repository check", "independent review with no P0 P1 or P2"]
remaining_risks: ["The evaluated capability is the bounded Stage 2 journey over the accepted select-evidence and one-clip inward-trim slices; it does not claim unsupported editing semantics."]
---

# WP-CA-UX-001 R2 complete evaluation

The v21 journey repairs the exact gap that invalidated R1: after creating and
previewing the new scoped revision, the same Electron journey explicitly
rejects it, shows `修订已拒绝`, retains the exact Permission Decision and proves
the Timeline stays at v6 before recovery begins.

| Dimension | Result | Current evidence |
| --- | --- | --- |
| Clarity | passed | Four labeled views share one workspace identity; two Direction and two Story candidates remain distinguishable. |
| Control | passed | Exact actions use native main confirmation and Host authority; a dual-ID target mismatch is rejected before writes. |
| Visible result | passed | The current three-second Preview plays; Review shows the execution-bound Preview, Master and passed QC before the scoped decision. |
| Decision closure | passed | The new revision is visibly rejected in the same journey, with exact Permission Decision and unchanged Timeline. |
| Failure closure | passed | Invalid payloads and stale digests fail closed; recovery marks the old Render and feedback intent expired. |
| Recovery | passed | Undo reaches v7, redo reaches v8, local/media preview state clears, and reopen reproduces Timeline v8 and the exact workspace digest. |

Root visual review accepts the current result, and independent review reports
no P0, P1 or P2. The user's earlier direct acceptance of the same Product UI
remains applicable because this repair changed action authority and proof, not
the visible workspace design.
