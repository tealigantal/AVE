---
evidence_id: EVD-20260811-WP-PRESET-002-COMPLETE
date: 2026-08-11
work_package_id: WP-PRESET-002
repository_commit: worktree-before-pr-update
code_fingerprint: 83e3fc15fa8c3e1d70bc2028fed99b30cd17ef7430ed87bf9f40c0035ca2633f
capability_ids: [CAP-TL-001, CAP-KF-001, CAP-XFORM-001, CAP-COMP-001, CAP-TIME-001, CAP-TRANS-001, CAP-COLOR-001, CAP-MASK-001, CAP-TEXT-001, CAP-AUDIO-001, CAP-RENDER-001, CAP-PRESET-001]
acceptance_ids: [ACC-001, ACC-002, ACC-003, ACC-004, ACC-005, ACC-006, ACC-007, ACC-008, ACC-009, ACC-010, ACC-011, ACC-012, ACC-013, ACC-014, ACC-015, ACC-016, ACC-017, ACC-018, ACC-019, ACC-020, ACC-021, ACC-022, ACC-023, ACC-024, ACC-025, ACC-026, ACC-027]
commands: ["pnpm run typecheck", "pnpm run contracts:check", "pnpm run contracts:compatibility", "pnpm run contracts:clean", "pnpm run edit-ir:test", "pnpm run timeline-core:test", "pnpm run render-graph:test", "pnpm run commit-plan:test", "pnpm run timeline:host:test", "pnpm run timeline-render:test", "pnpm run storage:check:raw", "pnpm run undo-redo:test", "pnpm run project-recovery:test", "pnpm run architecture", "pnpm run architecture:test", "pnpm run project-host:boundary", "pnpm run electron:runtime:test", "pnpm run acceptance:final:synthetic", "pnpm run acceptance:basic-vlog:real-review", "pnpm audit --audit-level high", "pnpm run check", "pnpm run docs:check"]
result: passed_full_repository_synthetic_real_media_security_and_independent_review
environment: "Windows local checkout; licensed CC BY 3.0 media, derivatives and review frames remained local; no media or local path committed"
artifacts: ["local-review-bundle:AVE-preset-review-20260811-v11", "preview.mp4 sha256:58b2f5349f07d5b3815598fb0dff64c8e7484361bf94b886833a5a79ae0e7c73", "master.mp4 sha256:5f735d7bc6f444bc58ac710812dd9174bb6772028897cc4e17a1fbef8a4b6e33", "candidate_source_identity_hash:f416ee927285f9d2532de553d641cbd17e52bd7a399e4b7b730fbacd3a924e39", "actual_source_identity_hash:2e2b96910e721cbfefaf6e6b32e86a5df100b073c6da5ff74d2026ac8b489a68", "candidate_preview_plan_id:plan-preview-9ceedd8d253a9624070c9687", "actual_preview_plan_id:plan-preview-bc7ea75ec5199f44ca95d828", "candidate_master_plan_id:plan-master-0f557099be0e9c836f23769f", "actual_master_plan_id:plan-master-14ab627c5179fe166e4fbda5", "verified_semantic_links:16"]
remaining_risks: ["External Marketplace execution, third-party executable Skills, Graphic Bake and AI Asset backends remain unavailable and fail closed.", "Blocked editing capabilities and existing Debts outside CAP-PRESET-001 are not promoted by this repair."]
---

# WP-PRESET-002 completion evidence

The five adversarial findings were reproduced against the original PR head and are now covered by negative regressions. A trusted project-local definition that binds the audited Basic Vlog compiler while declaring only static reframe is blocked with `PRESET_COMPILER_UNDECLARED_EFFECT`; the resolution returns no Commands and Project Host leaves the Timeline version unchanged. Preset compilers can emit only the exhaustive five-command primitive setter union; container Commands capable of carrying nested effects fail with `PRESET_COMPILER_COMMAND_FORBIDDEN`. Every admitted emitted capability must belong to both the compiler attestation and the exact selection's routed definition authority.

Preset JSON Schemas remain the sole protocol source. Contract generation now resolves local `$defs` references into valid TypeScript/Python types and produces dependency-free AJV standalone validators for the packaged Electron runtime; Preset Core consumes the generated types instead of handwritten protocol copies. Project Host validates external definitions, Creative Skill inputs and persisted application records through those Contract Runtime validators. Numeric, boolean and empty bindings, invalid IDs, non-finite values, illegal enums and unknown fields fail at the contract boundary before compilation.

Project Host no longer synchronously hashes all registered media. Preset policy queries only asset IDs declared by the selected definitions and consumes persisted Worker fingerprint, verification state, stat and probe facts. An instrumented filesystem regression proves an asset-free Preset performs zero reads or stat calls against 64 unrelated registered paths. Candidate Preview/Master validation resolves real Timeline asset locations and audio facts through the same Graph/ExecutionPlan resolver as formal rendering. Verified Original plus a mapped Proxy at another timebase yields distinct accurate Preview/Master plans; no-audio, muted-only, solo-excluded, Original/Proxy audio-divergent, duplicate-routing, missing-Original and proxy-only-without-map cases block accurately without Timeline mutation. Audio role derivation uses the same first-routing-per-clip rule as RenderGraph. Formal render rejects duplicate asset identities, ignores caller audio claims, re-probes actual Original/Proxy through Worker, verifies persisted Original authority and recorded semantic nodes, and derives linkage/QC/result refs from one unique source Map before storing candidate-to-actual source/plan linkage in both output manifests. The Project Host architecture guard rejects reintroduction of `readFileSync` media reads.

Atomic Timeline artifact registration rejects caller metadata for `object_ref_id`, `object_type`, `version`, `relation_key` and `byte_length` before staging. Authoritative descriptors win during insertion and commit events derive references from inserted rows. Each malicious field regression leaves object references, events and Timeline version unchanged.

Focused Preset Core, Host, formal Timeline render and type gates passed on the final linkage implementation. The 8.9-second licensed real-media v11 bundle exercised Worker fingerprint/probe, persisted Original authority, two generic Basic Vlog selections, candidate semantic preflight, Worker re-probe of actual Original/Proxy, Preview and original-backed Master render, 16 verified application-to-actual-plan links, reopen and QC. Candidate and actual source hashes and Preview/Master plan IDs remain explicitly distinct in both output manifests. Master measured -14.37 LUFS and -1.27 dBTP, Dialogue-to-Music Ducking was applied, and the encoded Preview/Master hashes exactly match the previously inspected accepted composition/caption outputs.

The complete repository check passed under the recorded fingerprint, including documentation, architecture, contracts, storage, Worker, Timeline, Host and formal render gates. `edit-ir:test` and synthetic final acceptance passed separately without claiming real media; the high-severity dependency audit found no known vulnerabilities. Independent read-only reviewers confirmed the compiler/contract boundary and render/storage boundary have no remaining code merge-blockers. The Evidence facts are final; work-package completion and generated-document closure are the remaining mechanical governance steps.
