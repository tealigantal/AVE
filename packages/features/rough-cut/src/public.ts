export const featureId = "rough-cut" as const;
import type { RoughCutPatch } from "../../../core/editorial-core/src/public.js";
export type RoughCutCommand = Readonly<{ type: string; payload: unknown }>;
export type RoughCutQuery = Readonly<{ type: string; parameters?: Readonly<Record<string, unknown>> }>;
export type RoughCutFeatureDescriptor = Readonly<{ feature_id: typeof featureId; label: "rough cut"; owner: "project-host"; layers: readonly ["commands", "queries", "use-cases", "policies", "validators", "prompts", "ports"] }>;
export const descriptor: RoughCutFeatureDescriptor = Object.freeze({ feature_id: featureId, label: "rough cut", owner: "project-host", layers: ["commands", "queries", "use-cases", "policies", "validators", "prompts", "ports"] as const });
export function validateRoughCutPatch(patch: RoughCutPatch, currentVersion: number, knownClipIds: ReadonlySet<string>): void { if (patch.base_version !== currentVersion) throw new Error("rough cut base version conflict"); if (patch.operations.length === 0) throw new Error("rough cut patch is empty"); for (const operation of patch.operations) { if (!knownClipIds.has(operation.clip_id)) throw new Error("rough cut target clip is unknown"); if ((operation.operation === "replace" || operation.operation === "j_cut" || operation.operation === "l_cut") && (operation.source_start_pts === undefined || operation.source_end_pts === undefined || operation.source_end_pts <= operation.source_start_pts)) throw new Error("rough cut source range is invalid"); if ((operation.operation === "j_cut" || operation.operation === "l_cut") && operation.audio_offset_pts === undefined) throw new Error("J/L cut requires audio offset"); } }
